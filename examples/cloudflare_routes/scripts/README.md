# Helper Script

`cf.sh` script is not usable directly as it has other dependencies.

It is here to demonstrate calling a script.

In `host-actions.yaml` replace:

```
- source /Users/Shared/Projects/Docker/telegram-bot/storage/scripts/cf.sh
```

with path to a script you want to call.
